<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 오버레이(팝업) 권한: 시스템 위에 메모 입력 창 표시용 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PickUpMemo">

        <!-- 메인 허브 Activity (LAUNCHER) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 메모 목록 Activity -->
        <activity
            android:name=".ui.MemoListActivity"
            android:exported="false" />

        <!-- 메모 편집 Activity -->
        <activity
            android:name=".ui.MemoEditActivity"
            android:exported="false" />

        <!-- 팝업 검증 테스트 Activity (FEAT-10) -->
        <activity
            android:name=".ui.TestActivity"
            android:exported="false" />

        <!-- 접근성 서비스 (배민커넥트 화면 감지용) -->
        <service
            android:name=".service.PickupAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
