# JVM 인자 — AGP 8.x 권장값
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX 사용 (필수)
android.useAndroidX=true

# Kotlin 코드 스타일
kotlin.code.style=official

# R 클래스를 모듈별 비전이(non-transitive)로 생성 (AGP 8 기본 권장)
android.nonTransitiveRClass=true
